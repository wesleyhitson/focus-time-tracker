<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FocusTimeTracker"
             xmlns:converters="clr-namespace:FocusTimeTracker.Converters"
             x:Class="FocusTimeTracker.App">
    <Application.Resources>
        <ResourceDictionary>
            
            <!-- Converters -->
            <converters:StringNotNullOrEmptyConverter x:Key="StringNotNullOrEmptyConverter"/>
            <converters:SelectedButtonConverter x:Key="SelectedButtonConverter"/>
            <converters:MinutesToProgressConverter x:Key="MinutesToProgressConverter"/>

            <!-- Colors -->
            <Color x:Key="Primary">#7C3AED</Color>
            <Color x:Key="Secondary">#3B82F6</Color>
            <Color x:Key="Tertiary">#10B981</Color>
            <Color x:Key="Accent">#F59E0B</Color>
            <Color x:Key="Background">#0F0F1E</Color>
            <Color x:Key="Surface">#1E1E2E</Color>
            <Color x:Key="SurfaceVariant">#2D2D3A</Color>
            <Color x:Key="TextPrimary">#FFFFFF</Color>
            <Color x:Key="TextSecondary">#A0A0A0</Color>
            <Color x:Key="TextTertiary">#6B6B7B</Color>
            <Color x:Key="Success">#4CAF50</Color>
            <Color x:Key="Warning">#FF9800</Color>
            <Color x:Key="Error">#EF4444</Color>

            <!-- Styles -->
            <Style TargetType="Page" ApplyToDerivedTypes="True">
                <Setter Property="BackgroundColor" Value="{StaticResource Background}"/>
            </Style>

            <Style TargetType="NavigationPage">
                <Setter Property="BarBackgroundColor" Value="{StaticResource Surface}"/>
                <Setter Property="BarTextColor" Value="{StaticResource TextPrimary}"/>
            </Style>

            <Style TargetType="Shell" ApplyToDerivedTypes="True">
                <Setter Property="Shell.BackgroundColor" Value="{StaticResource Surface}"/>
                <Setter Property="Shell.ForegroundColor" Value="{StaticResource TextPrimary}"/>
                <Setter Property="Shell.TitleColor" Value="{StaticResource TextPrimary}"/>
                <Setter Property="Shell.TabBarBackgroundColor" Value="{StaticResource Surface}"/>
                <Setter Property="Shell.TabBarForegroundColor" Value="{StaticResource TextSecondary}"/>
                <Setter Property="Shell.TabBarTitleColor" Value="{StaticResource TextSecondary}"/>
                <Setter Property="Shell.TabBarUnselectedColor" Value="{StaticResource TextSecondary}"/>
            </Style>

            <Style TargetType="Label">
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}"/>
                <Setter Property="FontFamily" Value="OpenSansRegular"/>
            </Style>

            <Style TargetType="Button">
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}"/>
                <Setter Property="FontFamily" Value="OpenSansSemibold"/>
                <Setter Property="BackgroundColor" Value="{StaticResource Primary}"/>
                <Setter Property="CornerRadius" Value="8"/>
            </Style>

            <Style TargetType="Entry">
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}"/>
                <Setter Property="PlaceholderColor" Value="{StaticResource TextTertiary}"/>
                <Setter Property="FontFamily" Value="OpenSansRegular"/>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
